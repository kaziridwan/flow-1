name = "flow-1"
compatibility_date = "2024-12-01"

# Static SPA served via Cloudflare Workers Static Assets.
# `wrangler deploy` uploads ./dist; unmatched routes fall back to index.html.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
